services:
  - type: web
    name: vocal-accompaniment-ai
    env: python
    buildCommand: pip install -r vocal_accompaniment/requirements.txt
    startCommand: uvicorn vocal_accompaniment.app:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
